# Trevish254 profile

A static creative-technology profile inspired by the supplied GitHub-style reference.

Open `index.html` directly in a browser, or serve this folder with any static file server.
